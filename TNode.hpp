#pragma once
/**The structure creates a new list with nodes of tree*/
struct TNode
{
	TNode* next; ///< pointer to the next element in the list
	int v; ///< vertex number 
	float weight; ///< weight of node
};

